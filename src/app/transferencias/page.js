import { Transferencias } from '@/sections';
export const metadata = {
	title: 'Transferencias - Banking',
	description: 'Transferi tu plata a la cuenta que desees.'
};

export default function TransferenciasPage() {
	return <Transferencias />;
}